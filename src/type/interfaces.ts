
interface EveryData {
  ein: string;
  name: string;
  description: string;
  profileUrl?: string;
  logoUrl?: string;
  coverImageUrl: string;
  matchedTerms: string[];
  slug: string;
  location: string;
  tags: string[];
}

export default EveryData;